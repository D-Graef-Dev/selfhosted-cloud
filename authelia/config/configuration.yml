server:
  endpoints:
    authz:
      forward-auth:
        implementation: 'ForwardAuth'

log:
  level: 'info'
  format: 'json'

totp:
  issuer: '{{ mustEnv "DOMAIN" }}'

authentication_backend:
  file:
    path: '/config/users_database.yml'

access_control:
  default_policy: 'deny'
  rules:
    - domain: 'grafana.{{ mustEnv "DOMAIN" }}'
      policy: 'two_factor'

session:
  cookies:
    - name: 'authelia_session'
      domain: '{{ mustEnv "DOMAIN" }}'
      authelia_url: 'https://auth.{{ mustEnv "DOMAIN" }}'
      default_redirection_url: 'https://grafana.{{ mustEnv "DOMAIN" }}'
      expiration: '1 hour'
      inactivity: '5 minutes'

regulation:
  modes:
    - 'ip'
  max_retries: 3
  find_time: '5 minutes'
  ban_time: '15 minutes'

storage:
  local:
    path: '/data/db.sqlite3'

notifier:
  filesystem:
    filename: '/data/notification.txt'
